{
  "version": 2,
  "builds": [
    {
      "src": "backend/dist/app.js",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "cd backend && npm install && npm run build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "backend/dist/app.js"
    }
  ]
}